//! `immich-federation-at-home` — mirror a shared Immich album into a local album.
//!
//! This is currently a placeholder entry point; the real startup sequence and sync loop
//! are implemented in later steps of `PLAN.md`.

#[tokio::main]
async fn main() -> anyhow::Result<()> {
    Ok(())
}
